import 'package:flutter/material.dart';

class LoveSpreadScreen extends StatelessWidget {
  const LoveSpreadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '연애운 보기 기능 화면',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
